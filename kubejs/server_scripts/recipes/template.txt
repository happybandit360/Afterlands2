onEvent('recipes', (event) => {

    var shapedModRecipes = [
        /*
        shapedRecipe(result, pattern[], key{}, id),
        shapedRecipe(Item.of('byg:pollen_block', 1), ['AA', 'AA'], {
            A: 'byg:pollen_dust'
        }),
        /*

    ];

    var shapelessModRecipes = [
        /*
        shapelessRecipe(result, ingredients[], id),
        shapelessRecipe(Item.of('projectvibrantjourneys:twigs', 4), ['#minecraft:leaves', '#forge:shears']),
        */
    ];


    shapedModRecipes.forEach(function (recipe) => {
        if (recipe.id) {
            event.shaped(recipe.result, recipe.pattern, recipe.key).id(recipe.id);
        } else {
            event.shaped(recipe.result, recipe.pattern, recipe.key);
        }
    });

    shapelessModRecipes.forEach((recipe) => {
        recipe.id
            ? event.shapeless(recipe.result, recipe.ingredients).id(recipe.id)
            : event.shapeless(recipe.result, recipe.ingredients);
    });

});